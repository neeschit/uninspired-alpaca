const defaultPeriodForDI = 14;

const getPositiveDirectionIndex = (bars, period = defaultPeriodForDI) => {};

const getNegativeDirectionIndex = (bars, period = defaultPeriodForDI) => {};

const getAverageDirectionalIndex = (bars, period = defaultPeriodForDI) => {};

module.exports = {
    getAverageDirectionalIndex,
    getNegativeDirectionIndex,
    getPositiveDirectionIndex
};
